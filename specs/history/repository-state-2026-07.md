# Historical Repository State: July 2026

> **Historical record.** The original `STATE.md` was last updated on 2026-07-06
> and accumulated point-in-time implementation claims, local process IDs,
> screenshots, installed paths, package sizes, source counts, prices, and test
> totals. Those values became stale as the product moved from a monolithic
> `meter.py` to the package architecture under `token_meter/`.

The former snapshot is intentionally not maintained as a current ledger. Use:

- [../ARCHITECTURE.md](../ARCHITECTURE.md) for current component ownership,
  data flow, extension rules, packaging, and privacy invariants;
- [../AGENTS.md](../AGENTS.md) for commands and installed-runtime proof;
- the feature specifications under `specs/` for accepted requirements and
  decisions;
- Git history for the exact state and validation evidence at a commit.

This short index preserves why the old ledger was retired without repeating
volatile facts that would become misleading again.
